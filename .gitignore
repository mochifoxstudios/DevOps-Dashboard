# Agent local state — generated at runtime, never commit
agent/.env
agent/.brain-state.json
agent/.audit.log
agent/.audit-*.log
agent/.audit-tip
agent/.ai-keys.json
agent/.salt
agent/.github-queue.json
agent/node_modules/
agent/fixtures/*.log
agent/fixtures/*.tmp

# Brainstorming + planning artifacts (regenerated)
.superpowers/

# Local editor/tooling config (preview launch configs, etc.)
.claude/

# OS / editor noise
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo
