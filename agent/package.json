{
  "name": "devops-local-agent",
  "version": "1.0.0",
  "private": true,
  "description": "Local backend agent for the DevOps Local dashboard. Restricted to a single workspace root; refuses access outside it.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "chokidar": "^5.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "turndown": "^7.2.4"
  },
  "optionalDependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.600.0"
  }
}
